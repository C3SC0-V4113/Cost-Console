// Empty stub for the `server-only` marker package in the Vitest environment.
// `server-only` only resolves under the React Server `react-server` export
// condition, which Vitest does not set, so server-only modules are aliased here.
export {};
