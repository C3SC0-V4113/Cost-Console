/** Cost Console project slug in Identity-Service. */
export const PROJECT_SLUG = 'cost-console';

/** Session cookie name relayed by the BFF. */
export const SESSION_COOKIE_NAME = 'identity_service_session';
